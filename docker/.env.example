# Copy to docker/.env and fill in. docker compose reads this automatically.
# See docker/README.md for how to generate the secret files it references.

BETTER_AGENT_USERNAME=changeme
BETTER_AGENT_BACKEND_PORT=18765
