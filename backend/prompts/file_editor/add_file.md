<file-editor-add-file path="{path}">
The file `{path}` was just added to your editing set. Read it; the
same rules apply and you may now edit it alongside the existing files.
The full set is now:

{file_list}
</file-editor-add-file>
